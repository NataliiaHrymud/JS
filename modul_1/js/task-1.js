'use strict';

const name = 'Генератор защитного поля';
let price;
let stringMessage;

price = 1000;
stringMessage = `Выбран ${name}, цена за штуку ${price} кредитов.`;
    console.log (stringMessage);
price = 2000;
stringMessage = `Выбран ${name}, цена за штуку ${price} кредитов.`;
    console.log(stringMessage);
